'use client';
import{useState}from'react';import{compressImageFile}from'@/lib/compress-image';import{createPaymentRequest}from'../actions';
export function RequestForm(){const[busy,setBusy]=useState('');const[amount,setAmount]=useState('');async function scan(files:FileList|null){if(!files?.[0])return;if(files.length>5){setBusy('영수증은 최대 5장까지 가능합니다.');return}try{setBusy('영수증 금액 인식 중…');const{recognize}=await import('tesseract.js');const{data}=await recognize(files[0],'kor+eng');const nums=[...data.text.matchAll(/(?:₩|원|합계|금액|total)?\s*([0-9]{1,3}(?:[,\s][0-9]{3})+|[0-9]{4,})/gi)].map(x=>Number(x[1].replace(/[,\s]/g,''))).filter(n=>n>0&&n<100000000);if(nums.length)setAmount(String(Math.max(...nums)));setBusy(nums.length?'OCR 금액을 입력했습니다. 확인 후 수정할 수 있습니다.':'금액을 찾지 못했습니다. 직접 입력해 주세요.')}catch{setBusy('OCR 인식에 실패했습니다. 금액을 직접 입력해 주세요.')}}async function submit(fd:FormData){try{setBusy('사진 압축 중…');const files=await Promise.all(fd.getAll('receipts').filter((v):v is File=>v instanceof File).map(compressImageFile));fd.delete('receipts');files.forEach(f=>fd.append('receipts',f));setBusy('등록 중…');await createPaymentRequest(fd)}catch(e){setBusy(e instanceof Error?e.message:'처리하지 못했습니다.')}}return <form action={submit} className="finance-request-form"><label>비목<select name="category">{['행사비','식비','교통비','물품비','교육비','기타'].map(x=><option key={x}>{x}</option>)}</select></label><label>금액<input name="amount" type="number" min="1" required value={amount} onChange={e=>setAmount(e.target.value)} placeholder="영수증의 최종 결제금액"/></label><label>내 계좌번호<input name="bank_account" required placeholder="은행명 계좌번호 예금주"/></label><label>영수증 사진<input name="receipts" type="file" accept="image/jpeg,image/png,image/webp" multiple required onChange={e=>void scan(e.target.files)}/></label><label>내용<textarea name="memo"/></label><button disabled={busy.endsWith('중…')}>결제 요청</button>{busy?<p>{busy}</p>:null}</form>}
